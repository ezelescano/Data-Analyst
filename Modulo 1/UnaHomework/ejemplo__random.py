#importamos el módulo random (esto tiene que estar arriba de todo)
import random
#genero un número entero al azar, entre paréntesis se explicita el rango, que incluye dichos valores.
numero = random.randint(0, 100)
#imprimo para probar
print(numero)
